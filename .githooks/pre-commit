#!/bin/bash
# Pre-Commit: Regression-Guard blockt unautorisierten Symbol-Verlust.
# Absichtliche Löschungen: git commit --no-verify
REPO_ROOT="$(git rev-parse --show-toplevel)"
PY="$REPO_ROOT/.venv/bin/python"
[[ -x "$PY" ]] || PY=python3.12
exec "$PY" -m collect.regression_guard --staged
